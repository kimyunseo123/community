<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="유기동물등록"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMMUNITY"
        tools:targetApi="31">
        <activity android:name=".HomeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DifficultyHardActivity" android:label="난이도 상" />
        <activity android:name=".DifficultyMediumActivity" android:label="난이도 중" />
        <activity android:name=".DifficultyEasyActivity" android:label="난이도 하" />
        <activity android:name=".QuizActivity" android:label="퀴즈페이지" />
        <activity android:name=".MypageActivity" android:label="마이페이지" />
        <activity android:name=".CenterListActivity" android:label="보호소찾기" />
    </application>

</manifest>